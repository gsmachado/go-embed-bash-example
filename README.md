# go-embed-bash-example

How to embed a bash script in a go binary and execute it, passing params, and checking its outcome codes (i.e., exit codes).

## Build & Run

```shell
go build -o ./bin/exec-script main.go
```

```shell
./bin/exec-script
```